services:
  redis:
    image: redis:alpine
    container_name: yt-rag-redis
    ports:
      - "6379:6379"
    restart: always
    command: ["redis-server", "--appendonly", "yes"]

networks:
  default:
    name: yt-rag-net
